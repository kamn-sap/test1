{
  "name": "test1-infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "cdk": "cdk",
    "synth": "cdk synth",
    "deploy": "cdk deploy",
    "deploy:local": "IS_LOCAL=true cdk deploy --endpoint http://localhost:4566",
    "bootstrap": "cdk bootstrap"
  },
  "devDependencies": {
    "aws-cdk-lib": "^2.100.0",
    "constructs": "^10.3.0",
    "typescript": "~5.3.0",
    "ts-node": "^10.9.0",
    "@types/node": "^20.0.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1600.0"
  }
}